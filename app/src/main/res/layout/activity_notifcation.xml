<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="stan.androiddemo.communication.Notification.NotificationActivity">

    <TextView
        android:id="@+id/txt_send_notif"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="@dimen/dp_10"
        android:text="Send Broadcast"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/txt_send_notif_local"
        app:layout_constraintTop_toBottomOf="@+id/txt_send_notif"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="@dimen/dp_10"
        android:text="Send Broadcast Local"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:orientation="vertical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/txt_send_notif_local"
        android:id="@+id/linearLayout_notif"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <Button
            android:id="@+id/btn_send_notif"
            android:layout_margin="@dimen/dp_10"
            android:padding="@dimen/dp_10"
            android:textColor="@android:color/holo_red_dark"
            android:text="发通知"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>

</android.support.constraint.ConstraintLayout>
